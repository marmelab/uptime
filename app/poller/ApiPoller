package poller














	var result = []byte(`{"Destination": "`+response.Destination+`"}`)
	log.Println(listOfDestination[i])
	req,error :=http.NewRequest("POST","http://localhost:8000/ips/results",bytes.NewBuffer(result))
	if(error!=nil){
		log.Fatal(error)
	}
	client := &http.Client{}
	resp,err := client.Do(req)
	if(err!=nil){
		log.Fatal(err)
	}
	defer resp.Body.Close()